# Fix extensible media settings saves

- Capture MediaClass description and video-setting payloads before application extension handlers run.
- Preserve the generic `mediaclass:save-settings` hook for application-owned settings persistence.
- Rebuild the distributed uploader bundle and cover the save ordering contract.

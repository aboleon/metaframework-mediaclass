# Unified media settings save

- Add an uploadable component slot for application-specific media settings.
- Present one translated save action for descriptions, video details, and extension settings.
- Emit a generic `mediaclass:save-settings` event while preserving the legacy save-button class.
- Rebuild the distributed Svelte uploader and document the extension contract.

# cabe Add: Vimeo embed provider

- Adds a built-in Vimeo `EmbedProvider` with canonical, player, channel, and group URL support.
- Preserves Vimeo unlisted-video hashes and renders responsive 16:9 player embeds.
- Registers the provider by default and adds package regression coverage.

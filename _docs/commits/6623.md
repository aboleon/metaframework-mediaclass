# Responsive provider embeds

- Adds typed aspect-ratio metadata for structured external video providers.
- Makes fluid TF1 embeds responsive while preserving explicit pixel heights.
- Documents the UI sizing behavior and adds package regression coverage.

# Use Laravel translation overrides

- Load package translations through Laravel’s `mfw-mediaclass` namespace.
- Support partial application overrides under `lang/vendor/mfw-mediaclass/{locale}/messages.php`.
- Publish language overrides only when `mediaclass:update --lang` is explicitly requested.
- Consolidate redundant README installation and update guidance and document localization upgrades.
- Cover package defaults, partial overrides, publishing paths, and updater behavior.
